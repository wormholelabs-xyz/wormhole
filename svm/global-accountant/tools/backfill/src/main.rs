//! `ga-backfill` CLI shim. The subcommand surface lands in later phases.

fn main() {
    eprintln!("ga-backfill: scaffolded but no subcommands implemented yet");
    eprintln!("see .claude/tasks/accountant-migration-orchestrator.md for the phase plan");
    std::process::exit(2);
}
